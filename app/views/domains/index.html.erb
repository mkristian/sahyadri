<h1>Listing domains</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @domains.each do |domain| %>
  <tr>
    <td><%=h domain.name %></td>
    <td><%= link_to 'Show', domain_path(domain.key) %></td>
    <td><%= link_to 'Edit', edit_domain_path(domain.key) %></td>
    <td><%= link_to 'Destroy', domain_path(domain.key), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New domain', new_domain_path %>
